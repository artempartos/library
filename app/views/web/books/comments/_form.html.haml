/= simple_form_for @comment, :html => { :class => 'form-horizontal' } do |f|
/  = f.input :text

/  .form-actions
/    = f.button :submit, :class => 'btn-primary'
/= link_to t('.cancel', :default => t("helpers.links.cancel")), book_comments_path, :class => 'btn'
