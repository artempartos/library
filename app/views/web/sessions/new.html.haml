.sessions-page
  %h1= t '.title'

  = simple_form_for @session, url: session_path(params.extract(:from)),
    input_html: {class: "form-horizontal"} do |f|
    = f.input :email
    = f.input :password
    = f.button :submit, class: 'btn-primary'

    = link_to t(".sing_in_now"), new_user_path
    = link_to t('.remind_password'), new_remind_password_path, class: "remind-password-link"
